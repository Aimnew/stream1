name: Deploy to Streamlit Cloud

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Test application
        run: |
          python -c "import streamlit; import pandas; import plotly"
          echo "✅ All dependencies installed successfully"

      - name: Deploy to Streamlit Cloud
        env:
          STREAMLIT_SHARING_TOKEN: ${{ secrets.STREAMLIT_SHARING_TOKEN }}
        run: |
          # Здесь можно добавить команды для деплоя на Streamlit Cloud
          # или другую платформу
          echo "🚀 Application ready for deployment"

      - name: Notify deployment
        run: |
          echo "✅ Application deployed successfully"
          echo "🌐 Check your app at: https://your-app-url.streamlit.app"
